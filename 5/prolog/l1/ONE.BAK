domains
    person = symbol
predicates
    male(person)
    female(person)
    married(person,person)
    son(person, person)
    daugter(person, person)
    mom(person, person)
    dad(person, person)
    sis(person, person)
    bro(person, person)
    uncle(person, person)
    aunt(person, person)
    %granny(person, person)
    %granddaddy(person, person)
    %kuzen(person, person)
    %kuzina(person, person)
    %pelmyannik(person, person)
    %pelmyannitsa(person, person)
    %testb(person, person)
    %tesha(person, person)
    %svekrov(person, person)
    %patent(person, person)
    %relative(person, person)

clauses
    male(vlad).
    male(zhenya).
    male(tolya).
    male(anatolyi).
    male(vova).
    male(serezha).
    male(ilya).
    male(nikita).
    male(viktor).
    female(natasha).
    female(sveta).
    female(nina).
    female(tanya).
    female(ulya).
    female(luba).

    married(zhenya, natasha).
    married(vova, ulya).
    married(serezha, tanya).
    married(tolya, sveta).
    married(anatolyi, nina).
    married(viktor, luba).

    dad(zhenya, vlad).
    dad(zhenya, dima).
    dad(vova, ilya).
    dad(serezha, nikita).
    dad(tolya, zhenya).
    dad(tolya, vova).
    dad(anatolyi, natasha).
    dad(anatolyi, serezha).
    mom(natasha, vlad).
    mom(natasha, dima).
    mom(ulya, ilya).
    mom(tanya, nikita).
    mom(sveta, zhenya).
    mom(sveta, vova).
    mom(nina, natasha).
    mom(nina, serezha).
    
    son(X, Y) :- male(X), dad(Y,X); male(X), mom(Y,X).
    son(X, Y) :- female(X), dad(Y,X); female(X), mom(Y,X).
    sis(X, Y) :- female(X), mom(SOME, X), mom(SOME, Y); female(X), dad(NAME, X), dad(NAME, Y).
    bro(X, Y) :- male(X), mom(SOME, X), mom(SOME, Y); male(X), dad(NAME, X), dad(NAME, Y).
    uncle(X, Y) :- male(X), mom(MOM, Y), sis(MOM,X); male(X), dad(DAD, Y), bro(DAD, X).
    aunt(X, Y) :- female(X), mom(MOM, Y), sis(MOM,X); female(X), dad(DAD, Y), sis(DAD, X).
   % granny(X, Y) :- female(X).
